{
    "name": "@rangkotodotcom/simdapo",
    "version": "1.0.1",
    "description": "Library Client for integration with simdapo",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "test": "echo \"No test specified\" && exit 0",
        "release:patch": "npm run build && npm version patch && git push --follow-tags && npm publish --access public",
        "release:minor": "npm run build && npm version minor && git push --follow-tags && npm publish --access public",
        "release:major": "npm run build && npm version major && git push --follow-tags && npm publish --access public"
    },
    "keywords": [
        "SIMDAPO",
        "NODE",
        "CLIENT"
    ],
    "author": "rangkotodotcom",
    "license": "ISC",
    "publishConfig": {
        "access": "public"
    },
    "dependencies": {
        "axios": "^1.10.0",
        "joi": "^17.13.3"
    },
    "devDependencies": {
        "@types/node": "^24.0.6",
        "@types/joi": "^17.2.3",
        "@types/axios": "^0.14.0",
        "typescript": "^5.8.3"
    }
}